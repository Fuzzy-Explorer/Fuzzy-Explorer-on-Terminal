check_shell=$(readlink /proc/$$/exe | grep bash)

if [ "$1" = "build" ]; then
  if [ -n "$check_shell" ]; then
    echo bash
  else
    . ~/.fet/fet_build.zsh
  fi
else
  if [ -n "$check_shell" ]; then
    ~/.fet/fet_main.bash
  else
    . ~/.fet/fet_main.zsh
  fi
  unset check_shell
fi
